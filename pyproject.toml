[project]
name = "pacuare-mcp-server"
version = "0.1.0"
description = "MCP server for Pacuare database queries"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pacuare>=0.1.0",
    "mcp>=1.0.0",
    "pandas>=2.0.0",
]
